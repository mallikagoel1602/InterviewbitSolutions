int Solution::isPalindrome(int A) {
    string Ai = to_string(A);
    int flag=0;
    for(int i=0; i<Ai.length()/2; i++){
        if(Ai[i]!=Ai[Ai.length()-1-i]){
            flag=1;
            break;
        }
    }
    if(flag==1)
        return 0;
    else
        return 1;
}
